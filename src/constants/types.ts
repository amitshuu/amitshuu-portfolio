export interface IService {
  serviceName: string;
  serviceIcon: string;
  serviceDescription: string;
}

export interface IProject {
  projectName: string;
  projectImage: string;
  projectDesc: string;
}
